[package]
name = "tantivy-library-bench"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tantivy = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"

[profile.release]
lto = true
codegen-units = 1
